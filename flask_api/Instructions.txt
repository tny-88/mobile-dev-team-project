Create a python virtual environment

use:
pip install -r requirements.txt 
to download the required libraries

use:
flask run
to run the server

Create a private key folder and put the firestore private key (.json file) in the folder